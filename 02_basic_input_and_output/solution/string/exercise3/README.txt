
주석을 참고하여 해당하는 C++ 변수 선언 또는 문장으로 완성하시오. 

C++에서는 함수/블록 { }의 처음 뿐만아니라 중간 위치에서도 변수를 선언할 수 있다.
하나의 블록에 동일한 변수를 두 번 이상 선언하지만 않으면 된다.

입출력 형식은 input.txt와 output.txt를 참고한다.

(참고) str + another_str, string s(" ... "), string str( another_str ), str == another_str


